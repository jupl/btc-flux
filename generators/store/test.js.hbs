'use strict';

var dispatcher = require('lib/dispatcher');
var id = require('{{dasherize-name}}');
var {{camelize name}} = dispatcher._source(id);

describe('{{humanize name}}', function() {
  it('', function() {

  });
});
